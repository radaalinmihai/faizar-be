{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "go: run watcher",
			"linux": {
				"command": "refresh run"
			},
			"windows": {
				"command": "nodemon --watch '${fileDirname}\\**\\*.go' --signal SIGTERM --exec 'go env -w CGO_ENABLED=1 && go run ${file}'"
			},
			"group": "build"
		}
	]
}